# Firebase Functions Environment Variables (Local Development)
#
# IMPORTANT: Do NOT set GCLOUD_PROJECT or FIREBASE_CONFIG in .env.
# These keys are reserved by Firebase and will cause emulator startup to fail.
# For production, use `firebase functions:config:set` instead.

# Database Configuration (optional for emulator)
# DATABASE_URL="https://<your-project>-default-rtdb.firebaseio.com/"
# FIRESTORE_DATABASE_ID="(default)"

# CORS config for local Angular dev server
CORS_ORIGIN="http://localhost:5000"

# Debug Configuration
NODE_ENV="development"
LOG_LEVEL="debug"

# API keys/secrets for any outbound integrations you own
# JWT_SECRET="your-jwt-secret-key-here"
# API_KEY="your-api-key-here"